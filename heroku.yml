build:
  docker:
    web: Dockerfile
    # worker: Dockerfile  # Removed for free hosting - no worker dyno needed

run:
  web:
    command: ['./entrypoint.sh', 'web']
    image: web
  # worker dyno removed for free hosting
  # Background tasks handled by APScheduler (in web process) + GitHub Actions
